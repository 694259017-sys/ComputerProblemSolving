

      void main() {
        IO.print("Number of Box");
        int box = Integer.parseInt(IO.readln());
        IO.print("Number of pencil");
        int pencil = Integer.parseInt(IO.readln());
        int total = box * pencil;
        IO.println("You have" + total + "pencil");
    }
